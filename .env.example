# JWT secret for signing tokens. Generate with: openssl rand -hex 32
JWT_SECRET=replace_me_with_a_strong_random_secret

# SQLite database path
DATABASE_PATH=./data/nordvault.db

# HTTP port
PORT=3000

# JWT expiration in seconds (default: 1 hour)
JWT_EXPIRES_IN=3600
